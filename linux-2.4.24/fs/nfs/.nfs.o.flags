ifeq (dir.o file.o flushd.o inode.o nfs2xdr.o pagelist.o proc.o read.o symlink.o unlink.o write.o nfsroot.o mount_clnt.o,$(strip $(subst $(comma),:,$(LDFLAGS) $(EXTRA_LDFLAGS) $(obj-y))))
FILES_FLAGS_UP_TO_DATE += nfs.o
endif
